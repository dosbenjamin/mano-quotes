@set('tag', as ?? 'a')

<{{ tag }}
  class="uppercase hover:underline {{ className ?? '' }}"

  @if(type)
    type="{{ type }}"
  @end

  @if(href)
    href="{{ href }}"
  @end

  @if(bindings?.['@click'])
    @click="{{ bindings['@click'] }}"
  @end
>
   {{{ await $slots.main() }}}
</{{ tag }}>
